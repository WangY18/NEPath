name: compas_opzuid
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - ipopt  # Nonlinear optimization solver (Gurobi replacement)
  - numpy>=1.21.0
  - cmake>=3.15
  - pip
  - pip:
    - nanobind>=1.3.2
    - scikit-build-core
    - pytest
